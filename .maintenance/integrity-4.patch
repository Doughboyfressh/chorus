diff --git a/src/lib/chorus/mcp-http.ts b/src/lib/chorus/mcp-http.ts
index 40e9f04..1906f8f 100644
--- a/src/lib/chorus/mcp-http.ts
+++ b/src/lib/chorus/mcp-http.ts
@@ -1,3 +1,4 @@
-import { dropSession, validSitId } from "./mcp-sitting";
-import { handleMcp, negotiateProtocol, type McpCtx } from "./mcp";
-import { clientKey, rateLimit } from "./limit";
+import { browserOriginAllowed, readJsonLimited } from "./http-body.ts";
+import { dropSession, validSitId } from "./mcp-sitting.ts";
+import { handleMcp, negotiateProtocol, type McpCtx } from "./mcp.ts";
+import { clientKey, rateLimit } from "./limit.ts";
@@ -6 +7 @@ const cors: Record<string, string> = {
-  "Access-Control-Allow-Origin": "*",
+  "Vary": "Origin",
@@ -113,0 +115 @@ export const mcpHandlers = {
+    if (!browserOriginAllowed(request)) return new Response("Forbidden", { status: 403 });
@@ -118,0 +121 @@ export const mcpHandlers = {
+    if (!browserOriginAllowed(request)) return new Response("Forbidden", { status: 403 });
@@ -140,2 +143,2 @@ export const mcpHandlers = {
-      body = await request.json();
-    } catch {
+      body = await readJsonLimited(request);
+    } catch (err) {
@@ -143 +146 @@ export const mcpHandlers = {
-        { jsonrpc: "2.0", id: null, error: { code: -32700, message: "Parse error" } },
+        { jsonrpc: "2.0", id: null, error: { code: -32700, message: err instanceof Error ? err.message : "Parse error" } },
@@ -145 +148 @@ export const mcpHandlers = {
-        400,
+        err instanceof Error && err.message === "Payload too large" ? 413 : 400,
diff --git a/src/lib/chorus/mcp-sitting.ts b/src/lib/chorus/mcp-sitting.ts
index 58673ff..031b248 100644
--- a/src/lib/chorus/mcp-sitting.ts
+++ b/src/lib/chorus/mcp-sitting.ts
@@ -0,0 +1,2 @@
+import { requireDurableDatabase } from "./durable-storage.ts";
+import { writeSnapshot } from "./snapshot-store.ts";
@@ -3 +5,4 @@ import { gradeArtifact } from "./grade.ts";
-import type { PreferencePair } from "./pairs.ts";
+import { diagnosticPair, type PreferencePair } from "./pairs.ts";
+import { boundedText, INTEGRITY_NOTE, MAX_ARTIFACT, quarantine } from "./integrity.ts";
+import { issueAttempt, consumeAttempt, invalidateAttempts, type ExamBinding } from "./attempts.ts";
+import { examFor } from "./grade.ts";
@@ -17,0 +23 @@ export type McpSitting = {
+  revision?: number;
@@ -29 +35 @@ export type McpSitting = {
-  exam?: { labId: string; level: number };
+  exam?: ExamBinding & { attemptId: string; expiresAt: number };
@@ -31,0 +38 @@ export type McpSitting = {
+    evaluation?: EvalResult;
@@ -44 +50,0 @@ const MAX_PAYLOAD = 400_000;
-const MAX_ARTIFACT = 24_000;
@@ -51,0 +58 @@ async function sqlClient() {
+    requireDurableDatabase(process.env);
@@ -55 +62 @@ async function sqlClient() {
-    return null;
+    throw new Error("Sitting storage is unavailable; no unverified fallback is permitted.");
@@ -63,2 +70,2 @@ async function loadFromDb(id: string): Promise<McpSitting | null> {
-    const rows = await sql<{ payload: string }>`
-      select payload from chorus_mcp_sittings
+    const rows = await sql<{ payload: string; revision: number }>`
+      select payload, revision from chorus_mcp_sittings
@@ -71 +78,2 @@ async function loadFromDb(id: string): Promise<McpSitting | null> {
-    if (!parsed?.id) return null;
+    if (parsed?.id !== id || !Array.isArray(parsed.scores) || !Array.isArray(parsed.pairs)) throw new Error("Invalid stored sitting");
+    parsed.revision = Number(rows[0].revision);
@@ -75 +83 @@ async function loadFromDb(id: string): Promise<McpSitting | null> {
-    return null;
+    throw new Error("Stored sitting could not be read safely.");
@@ -81 +89 @@ async function saveToDb(sitting: McpSitting) {
-  if (payload.length > MAX_PAYLOAD) return;
+  if (payload.length > MAX_PAYLOAD) throw new Error("Sitting is too large to record safely.");
@@ -85,5 +93 @@ async function saveToDb(sitting: McpSitting) {
-    await sql`
-      insert into chorus_mcp_sittings (id, payload, updated_at)
-      values (${sitting.id}, ${payload}, now())
-      on conflict (id) do update set payload = excluded.payload, updated_at = now()
-    `;
+    sitting.revision = await writeSnapshot(sql, sitting.id, payload, sitting.revision ?? 0);
@@ -99,2 +103,3 @@ async function saveToDb(sitting: McpSitting) {
-  } catch {
-    /* table missing in a fresh test process */
+  } catch (err) {
+    bySession.delete(sitting.id);
+    throw err;
@@ -141 +146 @@ export async function assertWriter(sessionId: string, writeKey?: string) {
-export async function markExam(sessionId: string, labId: string, level: number) {
+export async function markExam(sessionId: string, labId: string, level: number, artifact: string, userTest?: string) {
@@ -143 +148,10 @@ export async function markExam(sessionId: string, labId: string, level: number)
-  sitting.exam = { labId, level };
+  examFor(labId, level, userTest); // Reject unknown labs and invalid levels before changing state.
+  boundedText(artifact, "artifact", MAX_ARTIFACT, 8);
+  if (sitting.scores.length && (sitting.labId !== labId || sitting.level !== level)) {
+    throw new Error("Lab and level are pinned. Reset explicitly to start a different test.");
+  }
+  const binding = { labId, level, artifact, userTest };
+  const attempt = await issueAttempt(sessionId, binding);
+  sitting.labId = labId;
+  sitting.level = level;
+  sitting.exam = { ...binding, ...attempt };
@@ -145 +159,9 @@ export async function markExam(sessionId: string, labId: string, level: number)
-  return sitting;
+  return attempt;
+}
+
+export function examReady(sitting: Pick<McpSitting, "exam">, labId: string, level: number,
+  artifact: string, attemptId: string, userTest?: string) {
+  const exam = sitting.exam;
+  return Boolean(exam && exam.attemptId === attemptId && exam.expiresAt > Date.now() &&
+    exam.labId === labId && exam.level === level && exam.artifact === artifact &&
+    (exam.userTest ?? "") === (userTest ?? ""));
@@ -148,2 +170,7 @@ export async function markExam(sessionId: string, labId: string, level: number)
-export function examReady(sitting: { exam?: { labId: string; level: number } }, labId: string, level: number) {
-  return sitting.exam?.labId === labId && sitting.exam.level === level;
+export async function consumeExam(sessionId: string, binding: ExamBinding, attemptId: string) {
+  const current = await sittingFor(sessionId);
+  if (!examReady(current, binding.labId, binding.level, binding.artifact, attemptId, binding.userTest)) return false;
+  if (!await consumeAttempt(sessionId, attemptId, binding)) return false;
+  delete current.exam;
+  await saveToDb(current);
+  return true;
@@ -162,12 +189,3 @@ export async function sittingFor(sessionId: string, labId = "prompt"): Promise<M
-export function writerRanExam(executor?: string) {
-  const e = (executor ?? "").trim().toLowerCase();
-  if (!e || e.length < 6) return true;
-  if (
-    /^(self|host|same|this|writer|chorus|mcp[- ]?host|grok|claude|sonnet|opus|gemini|chatgpt|openai|anthropic|xai)([-_.].*)?$/.test(
-      e,
-    )
-  ) {
-    return true;
-  }
-  if (/\b(grok|claude|chatgpt|gpt-?[45]|gemini|sonnet|opus)\b/.test(e)) return true;
-  return false;
+export function writerRanExam(_executor?: string) {
+  // A user-supplied name cannot establish that a different model ran anything.
+  return true;
@@ -177 +195 @@ export async function recordScore(sessionId: string, artifact: string, graded: E
-  const clipped = artifact.slice(0, MAX_ARTIFACT);
+  boundedText(artifact, "artifact", MAX_ARTIFACT, 8);
@@ -178,0 +197,4 @@ export async function recordScore(sessionId: string, artifact: string, graded: E
+  if (sitting.scores.length >= 8) return { sitting, pair: null, graded, capped: true as const };
+  if (sitting.scores.length && (sitting.labId !== graded.labId || sitting.level !== graded.level)) {
+    throw new Error("Lab and level are pinned. Reset explicitly to start a different test.");
+  }
@@ -180,8 +202 @@ export async function recordScore(sessionId: string, artifact: string, graded: E
-  const contaminated = writerRanExam(sitting.executor);
-  sitting.labId = graded.labId;
-  sitting.level =
-    graded.score >= 100 && graded.failed.length === 0 && !graded.exhausted
-      ? graded.level + 1
-      : graded.level;
-  sitting.current = clipped;
-  if (!sitting.artifact0) sitting.artifact0 = clipped;
+  const evaluation = quarantine({ ...graded, executionContext: JSON.stringify(["mcp-host:unverified", sitting.executor || "mcp-host"]) });
@@ -189,31 +204,12 @@ export async function recordScore(sessionId: string, artifact: string, graded: E
-  if (sitting.scores.length >= 8) {
-    await saveToDb(sitting);
-    return { sitting, pair: null, graded, capped: true as const };
-  }
-  sitting.scores.push({
-    artifact: clipped,
-    score: graded.score,
-    failed: graded.failed,
-    passed: graded.passed,
-    fixture: graded.fixture,
-    executor: sitting.executor,
-    contaminated,
-  });
-  const sameArtifact =
-    Boolean(prev) && prev!.artifact.replace(/\s+/g, " ").trim() === clipped.replace(/\s+/g, " ").trim();
-  let pair: PreferencePair | null = null;
-  if (prev && graded.score > prev.score && !sameArtifact) {
-    pair = {
-      prompt: (goal || sitting.labId).slice(0, 800),
-      rejected: prev.artifact,
-      chosen: clipped,
-      rejected_score: prev.score,
-      chosen_score: graded.score,
-      fixture: graded.fixture,
-      model: sitting.executor || "mcp-host",
-      labId: sitting.labId,
-      generation: sitting.scores.length,
-      contaminated,
-    };
-    sitting.pairs.push(pair);
-  }
+  const pair = prev ? diagnosticPair({
+    prompt: (goal || sitting.labId).slice(0, 800), rejected: prev.artifact, chosen: artifact,
+    previous: prev.evaluation, current: evaluation, model: sitting.executor || "mcp-host",
+    generation: sitting.scores.length + 1,
+  }) : null;
+  sitting.labId = graded.labId;
+  sitting.level = graded.score >= 100 && graded.failed.length === 0 && !graded.exhausted ? graded.level + 1 : graded.level;
+  sitting.current = artifact;
+  if (!sitting.artifact0) sitting.artifact0 = artifact;
+  sitting.scores.push({ artifact, evaluation, score: graded.score, failed: graded.failed,
+    passed: graded.passed, fixture: graded.fixture, executor: sitting.executor, contaminated: true });
+  if (pair) sitting.pairs.push(pair);
@@ -222 +218 @@ export async function recordScore(sessionId: string, artifact: string, graded: E
-  return { sitting, pair, graded, capped: false as const };
+  return { sitting, pair, graded: evaluation, capped: false as const };
@@ -227,11 +223,4 @@ function asEval(row: McpSitting["scores"][number], labId: string, level: number)
-  return {
-    labId,
-    fixture: row.fixture,
-    score: row.score,
-    passed: row.passed,
-    failed: row.failed,
-    evidence: executor,
-    level,
-    executor,
-    contaminated: row.contaminated ?? writerRanExam(row.executor),
-  };
+  return quarantine(row.evaluation ?? {
+    labId, fixture: row.fixture, score: row.score, passed: row.passed, failed: row.failed,
+    evidence: "Legacy result; original test identity and execution were not verified.", level, executor,
+  });
@@ -344,0 +334,3 @@ export async function sittingSnapshot(sessionId: string) {
+    verified: false,
+    integrityNote: INTEGRITY_NOTE,
+    cleanPairCount: 0,
@@ -372,0 +365 @@ export async function applySittingUpdate(
+  if (args.labId !== undefined) examFor(args.labId, 0);
@@ -377 +370 @@ export async function applySittingUpdate(
-    await resetSitting(sessionId, args.labId);
+    throw new Error("Lab is pinned. Use an explicit reset to change labs; existing history was not erased.");
@@ -426,0 +420,10 @@ export async function nextOrchestraSeat(sessionId: string) {
+  if (seat === "exam") {
+    const artifact = mergeDeliverable(sitting.orchestra) || sitting.current || "";
+    let attempt = sitting.exam;
+    if (!attempt || !examReady(sitting, sitting.labId, sitting.level, artifact, attempt.attemptId)) {
+      await markExam(sessionId, sitting.labId, sitting.level, artifact);
+      attempt = sitting.exam;
+    }
+    return { done: false, ...seatPrompt(sitting.orchestra, seat), attemptId: attempt?.attemptId,
+      note: "Return this attemptId with chorus_fill. This is unverified practice, not an independent exam." };
+  }
@@ -430 +433 @@ export async function nextOrchestraSeat(sessionId: string) {
-export async function fillOrchestraSeat(sessionId: string, seat: string, text: string) {
+export async function fillOrchestraSeat(sessionId: string, seat: string, text: string, attemptId?: string) {
@@ -439,0 +443,6 @@ export async function fillOrchestraSeat(sessionId: string, seat: string, text: s
+  if (pending === "exam") {
+    const artifact = mergeDeliverable(sitting.orchestra) || sitting.current || "";
+    if (!attemptId || !await consumeExam(sessionId, { labId: sitting.labId, level: sitting.level, artifact }, attemptId)) {
+      return { error: "Request chorus_next and submit its unused attemptId for this exact artifact." };
+    }
+  }
@@ -455 +463,0 @@ export async function fillOrchestraSeat(sessionId: string, seat: string, text: s
-    sitting.exam = { labId: sitting.labId, level: sitting.level };
@@ -470 +477,0 @@ export async function fillOrchestraSeat(sessionId: string, seat: string, text: s
-    recorded.sitting.exam = sitting.exam;
@@ -489 +496 @@ export async function sittingPairs(sessionId: string) {
-  return sitting?.pairs ?? [];
+  return (sitting?.pairs ?? []).map((row) => ({ ...row, contaminated: true, verified: false as const, integrityNote: INTEGRITY_NOTE }));
@@ -492,0 +500,2 @@ export async function resetSitting(sessionId: string, labId?: string) {
+  if (labId !== undefined) examFor(labId, 0);
+  await invalidateAttempts(sessionId);
@@ -500,0 +510 @@ export async function resetSitting(sessionId: string, labId?: string) {
+    revision: existing?.revision,
@@ -507,0 +518 @@ export async function dropSession(sessionId: string) {
+  await invalidateAttempts(sessionId);
diff --git a/src/lib/chorus/mcp-url.ts b/src/lib/chorus/mcp-url.ts
index 5609c9d..4c240d3 100644
--- a/src/lib/chorus/mcp-url.ts
+++ b/src/lib/chorus/mcp-url.ts
@@ -9 +9 @@ function newSit() {
-  else for (let i = 0; i < bytes.length; i++) bytes[i] = Math.floor(Math.random() * 256);
+  else throw new Error("A secure context with cryptographic randomness is required.");
@@ -14 +14 @@ export function validSitId(id: string) {
-  return /^[a-zA-Z0-9_-]{8,80}$/.test(id);
+  return /^(?:[a-f0-9]{32}|[a-f0-9]{8}-[a-f0-9]{4}-4[a-f0-9]{3}-[89ab][a-f0-9]{3}-[a-f0-9]{12})$/i.test(id);
diff --git a/src/lib/chorus/mcp.test.ts b/src/lib/chorus/mcp.test.ts
index d40cda9..c2fcccb 100644
--- a/src/lib/chorus/mcp.test.ts
+++ b/src/lib/chorus/mcp.test.ts
@@ -3 +3 @@ import { describe, it } from "node:test";
-import { handleMcp, negotiateProtocol } from "./mcp.ts";
+import { handleMcp as rawHandleMcp, negotiateProtocol } from "./mcp.ts";
@@ -6,0 +7,15 @@ import { dropSession } from "./mcp-sitting.ts";
+// Legacy happy-path cases use a valid client handshake. Security cases below
+// and integrity.test.ts invoke rawHandleMcp directly and never auto-create an exam.
+async function handleMcp(body: Parameters<typeof rawHandleMcp>[0], ctx: Parameters<typeof rawHandleMcp>[1]) {
+  const msg = body as { id?: number; method?: string; params?: { name?: string; arguments?: Record<string, unknown> } };
+  if (msg.method === "tools/call" && msg.params?.name === "chorus_score" && msg.params.arguments) {
+    const args = msg.params.arguments;
+    const res = await rawHandleMcp({ id: 999, method: "tools/call", params: { name: "chorus_exam", arguments: {
+      artifact: args.artifact, labId: args.labId, level: args.level, userTest: args.userTest,
+    } } }, ctx) as { result: { content: { text: string }[] } };
+    const exam = JSON.parse(res.result.content[0].text);
+    return rawHandleMcp({ ...msg, params: { ...msg.params, arguments: { ...args, attemptId: exam.attemptId } } }, ctx);
+  }
+  return rawHandleMcp(body, ctx);
+}
+
@@ -125 +140 @@ describe("handleMcp", () => {
-  it("marks a pair clean when executor is another model", async () => {
+  it("keeps a claimed independent executor unverified", async () => {
@@ -129 +144 @@ describe("handleMcp", () => {
-    await tool(sid, 12, "chorus_score", { labId: "prompt", artifact: "review this please" });
+    await tool(sid, 12, "chorus_score", { labId: "prompt", artifact: "review this please", executor: "local-llama" });
@@ -154 +169 @@ describe("handleMcp", () => {
-    assert.equal(payload.pair?.contaminated, false);
+    assert.equal(payload.pair?.contaminated, true);
@@ -161 +176 @@ describe("handleMcp", () => {
-    await tool(sid, 14, "chorus_score", { labId: "prompt", artifact: "review this please" });
+    await tool(sid, 14, "chorus_score", { labId: "prompt", artifact: "review this please", executor: "grok" });
@@ -313 +328 @@ describe("handleMcp", () => {
-    const res = (await tool(sid, 50, "chorus_score", { labId: "prompt", artifact: "review this please" })) as {
+    const res = (await rawHandleMcp({ id: 50, method: "tools/call", params: { name: "chorus_score", arguments: { labId: "prompt", artifact: "review this please" } } }, sid)) as {
